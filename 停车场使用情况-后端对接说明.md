# 停车场车位使用情况：前后端对接说明

前端已将数据看板的停车柱状图替换为概览、筛选、排序和表格，继续使用 `GET /api/v1/admin/stats/distribution` 的 `parking_remain`，没有增加接口调用，也没有引入业务演示数据。

## 前端已完成

- 概览展示全部返回停车场的总车位、已用、剩余、已满停车场数、高占用停车场数；不随局部筛选和排序变化。
- 默认按使用率从高到低展示前 8 个停车场；先筛选、排序，再截取 8 条。
- 筛选：全部、已满、高占用 ≥60%、有空位。
- 排序：使用率从高到低、使用率从低到高、剩余车位从多到少；同值按稳定 ID 排序，未知排序值置后。
- 表格展示名称及可选编号、使用率进度条、已用 / 总车位、剩余、状态；单行不提供点击或详情弹窗。
- 全部停车场数量大于 8 且账号有 `parking:view` 权限时，展示“查看全部 N 个”，跳转 `/parking-management`。局部筛选不改变该入口判断；8 个及以下隐藏入口。
- 支持空列表、筛选无结果、余位未知、容量为零以及亮/暗主题。

## 后端需要补充或确认

| 优先级 | 项目 | 要求及影响 |
| --- | --- | --- |
| 需确认 | `parking_remain` 必须是完整列表 | 当前前端从全部列表计算概览、停车场总数和全局排名。若接口已经返回全部停车场，无须新增接口；若当前仅返回 Top N 或有隐式上限，需要取消截断。仅返回 8 条无法支持全局升序、余位排序和准确总数。 |
| 原型字段补充 | 每项增加 `code` | 返回停车区真实业务编号，如 `P5`。前端已兼容 `code?: string \| null`，有值就显示在名称后，没有值就隐藏，不用数据库 ID 伪造业务编号。无需为编号单独查详情。 |
| 需确认 | `capacity`、`remain` 同口径、同快照 | `capacity` 为非负整数；已知的 `remain` 满足 `0 ≤ remain ≤ capacity`；两者属于同一停车场、同一统计时点。前端据此计算已用数量及精确使用率。 |
| 需确认 | 未知余位的含义 | 未知余位返回 `null`，不能用 `0` 代替。若希望完整展示概览，需要为所有有容量的停车场提供有效余位；余位未知时，前端将相关汇总显示为 `—` 并提示缺失数量。 |
| 需确认 | 列表统计范围 | 明确是否包含停用、关闭停车场，并保证列表、概览及收费类型分布采用一致范围。前端不额外猜测或过滤启停状态。 |

现有字段继续使用：`id`、`name`、`capacity`、`remain`、`usage`。`id` 应保持稳定，超出 JavaScript 安全整数范围的 ID 请返回字符串；`usage` 沿用 0–1 的比例协议。容量和余位齐全时，前端优先按数量计算使用率，避免后端百分比舍入导致错误排序或状态。

不需要后端新增“已用”“高占用”“已满”字段，也不需要单个停车场明细接口来支撑本组件。

如果未来停车场规模大到不能返回完整列表，可另行设计分页接口，同时返回全局概览、全部数量、筛选结果数量，并支持状态筛选、三种排序和每页 8 条；当前前端尚未接入这套可选分页方案。

## 展示口径

| 场景 | 前端处理 |
| --- | --- |
| `capacity > 0` 且 `remain = 0` | 已满，红色；使用率 100%。 |
| `remain > 0` 且 `60% ≤ 使用率 < 100%` | 高占用，橙色；计入高占用，不重复计入已满。 |
| `remain > 0` 且 `使用率 < 60%` | 空闲，绿色。 |
| “有空位”筛选 | 包含所有 `capacity > 0` 且 `remain > 0` 的停车场，包括高占用。 |
| `capacity = 0` | 显示“未配置车位”，使用率 `—`，不计入已满或高占用。 |
| `capacity > 0` 且 `remain = null` | 显示“余位未知”，已用和剩余 `—`；使用率可继续显示接口提供的比例；不归入已满、高占用或有空位筛选。 |
| 百分比精度 | 排序和状态用精确比例；文字保留最多一位小数，向下截断，避免 59.96% 显示为 60% 或 99.96% 显示为 100%。 |

本说明基于仓库现有接口适配代码整理；后端是否完整返回列表、当前统计范围和实时余位质量仍需联调确认。
