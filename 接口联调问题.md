# 接口联调问题

> 项目以已确认的页面原型和业务规则为准。下列差异需在联调时与后端确认；未确认前，前端继续按原型约束处理。

## 座位规划管理

| 模块 / 接口 | 差异或问题 | 当前前端处理 | 建议后端确认 |
| --- | --- | --- | --- |
| 分区列表 `GET /api/v1/admin/zones` | 原型支持检票口多选的 OR 查询，但接口没有检票口筛选参数；接口也未约定“楼层顺序 + 分区排序号”的排序。 | 每页 100 条自动拉取全部分顺，再在前端完成全局排序、检票口 OR 筛选和 20/50/100 条分页。 | 增加检票口 ID 多选参数（OR 语义），并明确稳定排序规则。 |
| 新增分区 `POST /api/v1/admin/zones` | 接口将编号、楼层、排号、排序和检票口定义为可选，原型要求这些字段必填，且至少绑定一个检票口。 | 按原型严格校验，并在创建时完整提交所有字段。 | 后端将这些字段调整为必填，并校验检票口数组非空。 |
| 编辑分区 `PATCH /api/v1/admin/zones/:id` | 接口允许修改分区编号，也允许必要业务字段为空；原型规定编号创建后不可修改，并始终至少绑定一个检票口。 | 编辑前读取详情，前端校验完整字段，更新请求不提交 `code`。 | 后端忽略或拒绝编号修改，并保持必填及检票口非空校验。 |
| 删除分区 `DELETE /api/v1/admin/zones/:id` | 接口文档未声明“仅停用分区可删除”，原型要求启用分区不允许删除。 | 前端会先拦截启用状态的删除操作。 | 后端同步校验分区状态，避免绕过前端后误删。 |
| 覆盖检票口 `PUT /api/v1/admin/zones/:id/gates` | 接口允许空数组清空绑定，与原型“每个分区至少一个检票口”冲突。 | 本轮未单独调用该接口，检票口绑定跟随分区新增/编辑一并提交。 | 禁止业务分区清空全部检票口，或明确仅哪些特殊场景可以清空。 |
| 分区导入/导出 | 接口提供 CSV，导出为全量且不接收当前筛选条件；原型要求 Excel、当前筛选导出，导入时还需重复数据覆盖/跳过选择及错误明细。 | 本轮不接入分区导入导出功能。 | 正式对接前确认文件格式、筛选参数、重复策略和错误返回结构。 |
