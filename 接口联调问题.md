# 接口联调问题

> 项目以已确认的页面原型和业务规则为准。下列差异需在联调时与后端确认；未确认前，前端继续按原型约束处理。

## 座位规划管理

| 模块 / 接口 | 差异或问题 | 当前前端处理 | 建议后端确认 |
| --- | --- | --- | --- |
| 分区列表 `GET /api/v1/admin/zones` | 原型支持检票口多选的 OR 查询，但接口没有检票口筛选参数；接口也未约定“楼层顺序 + 分区排序号”的排序。 | 每页 100 条自动拉取全部分顺，再在前端完成全局排序、检票口 OR 筛选和 20/50/100 条分页。 | 增加检票口 ID 多选参数（OR 语义），并明确稳定排序规则。 |
| 新增分区 `POST /api/v1/admin/zones` | 接口将编号、楼层、排号、排序和检票口定义为可选，原型要求这些字段必填，且至少绑定一个检票口。 | 按原型严格校验，并在创建时完整提交所有字段。 | 后端将这些字段调整为必填，并校验检票口数组非空。 |
| 编辑分区 `PATCH /api/v1/admin/zones/:id` | 接口允许修改分区编号，也允许必要业务字段为空；原型规定编号创建后不可修改，并始终至少绑定一个检票口。 | 编辑前读取详情，前端校验完整字段，更新请求不提交 `code`。 | 后端忽略或拒绝编号修改，并保持必填及检票口非空校验。 |
| 删除分区 `DELETE /api/v1/admin/zones/:id` | 接口文档未声明“仅停用分区可删除”，原型要求启用分区不允许删除。 | 前端会先拦截启用状态的删除操作。 | 后端同步校验分区状态，避免绕过前端后误删。 |
| 覆盖检票口 `PUT /api/v1/admin/zones/:id/gates` | 接口允许空数组清空绑定，与原型“每个分区至少一个检票口”冲突。 | 本轮未单独调用该接口，检票口绑定跟随分区新增/编辑一并提交。 | 禁止业务分区清空全部检票口，或明确仅哪些特殊场景可以清空。 |
| 分区导入/导出 | 接口提供 CSV，导出为全量且不接收当前筛选条件；原型要求 Excel、当前筛选导出，导入时还需重复数据覆盖/跳过选择及错误明细。 | 本轮不接入分区导入导出功能。 | 正式对接前确认文件格式、筛选参数、重复策略和错误返回结构。 |

## 检票口管理

| 模块 / 接口 | 差异或问题 | 当前前端处理 | 建议后端确认 |
| --- | --- | --- | --- |
| 检票口列表 `GET /api/v1/admin/gates` | 原型要求默认按排序号升序展示，但接口未约定稳定排序规则；服务端分页后前端无法保证跨页全局排序。 | 列表按接口返回的分页顺序展示；导出拉取全部分页后按排序号、编号排序。 | 明确列表默认按 `sort_order ASC, code ASC` 稳定排序。 |
| 新增检票口 `POST /api/v1/admin/gates` | 接口将 `code`、`floor_id`、`sort_order` 等字段定义为可选，但原型要求编号、楼层和定位必填，排序默认 1；`code` 可选时未说明是否自动生成。 | 按原型校验并完整提交；新增固定提交 `status=1`。 | 将原型必填字段调整为接口必填，并明确编号和排序默认规则。 |
| 检票口楼层字段 | 请求中的 `floor_id` 可选，但 `GateVO.floor_id`、`floor_name` 定义为非空，未说明省略楼层时的返回结果。 | 楼层使用 `GET /api/v1/admin/floors` 动态加载并强制选择。 | 统一请求与返回的可空约束。 |
| 检票口编号与名称校验 | 原型要求编号 2–10 位且唯一，现有前端还会拦截名称重复；接口文档未声明长度、唯一性及冲突错误码。 | 前端执行格式和当前页重复校验，最终仍以接口业务错误为准。 | 明确编号、名称唯一性和字段长度，并返回可定位字段的冲突错误。 |
| 更新检票口 `PATCH /api/v1/admin/gates/:id` | 文档说明编号忽略，但仍要求每次更新携带 `name/lng/lat`；缺少只更新开放三态的专用接口，快捷状态操作存在覆盖并发基础信息的风险。 | 快捷状态更新前先读取最新详情，再仅提交 `name/lng/lat/open_status/status_remark`。 | 增加状态专用接口，或允许 `PATCH` 真正按传入字段局部更新且不强制基础字段。 |
| 启用状态 `status` | 接口额外提供启用/停用状态及列表筛选，但检票口原型只定义开放/关闭/管制三态。 | 页面不展示启停；新增固定启用，普通编辑不提交 `status`，避免意外改变后端状态。 | 确认检票口是否确需独立启停能力及其与 `open_status`、`match_open` 的业务关系。 |
| 检票口导出 | 原型要求导出当前筛选结果 Excel，但 `GateController` 未提供导出接口。 | 前端按当前筛选拉取全部分页并生成 UTF-8 CSV，Excel 可直接打开。 | 若数据量增大或需标准 XLSX，补充服务端筛选导出接口。 |
| H5 状态同步 | 原型写明状态变更后约 5 分钟同步至 H5，接口文档未约定同步机制或时效。 | 继续展示原型中的 5 分钟提示。 | 确认同步链路、缓存刷新机制和可承诺的最大延迟。 |

## 交通管制管理

| 模块 / 接口 | 差异或问题 | 当前前端处理 | 建议后端确认 |
| --- | --- | --- | --- |
| 管制列表 `GET /api/v1/admin/controls` | 页面支持管制时间状态、日期范围筛选和全量地图，但接口仅支持关键字、发布状态、管制类型和数据来源，也未约定稳定排序。 | 将接口支持的条件提交后端，每页 100 条拉取全部结果，再按时间状态、日期交集进行前端筛选，并按置顶、排序号、开始时间排序。 | 增加时间状态/起止日期筛选参数，并明确默认稳定排序；数据量增大时需避免全量分页拉取。 |
| 管制导出 `GET /api/v1/admin/controls/export` | 接口不接受任何筛选参数，无法按页面当前条件导出。 | 使用后端原始 UTF-8 CSV，页面明确显示并提示“导出全部”。 | 增加与列表一致的筛选参数，或确认业务只需要全量导出。 |
| 定时发布 | 页面原有定时发布时间，但新增和编辑接口均不接受 `publish_at`；返回的 `publish_at` 仅能理解为实际发布时间。 | 保留“实际发布时间”只读项；新增固定创建草稿，只能通过发布接口手动发布。 | 确认交通管制是否需要定时发布；如需要，补充请求字段及调度失败状态。 |
| 管制区域 `geometry_json` | 文档只声明多边形 GeoJSON 文本，未声明坐标系；页面支持圆形、矩形和多边形。 | 按高德地图 GCJ-02 使用；提交时将三种形状统一转换为 GeoJSON Polygon，回读后统一显示为多边形。 | 明确坐标系、GeoJSON 版本、闭环要求及顶点数量/大小限制。 |
| 清空管制区域 `PATCH /api/v1/admin/controls/:id` | `ControlVO.geometry_json` 可为 null，但编辑请求只声明可选 string，未说明传 null 是否表示清空。 | 编辑清空时提交 `geometry_json: null`；若后端拒绝则直接展示错误。 | 明确字段省略、空字符串和 null 的更新语义，并支持显式清空。 |
| 发布人字段 | 返回仅有 `publisher_id`，页面需要展示发布人名称。 | 暂显示为 `用户 #<publisher_id>`，空值显示 `—`。 | 返回发布人姓名或嵌套用户摘要，避免前端额外逐条查询。 |
| 管制编号 | 新增接口的 `code` 可选，但未说明省略后的生成格式、唯一性和长度规则。 | 新增不提交编号，使用后端返回编号；编辑不提交编号。 | 明确自动编号规则、唯一约束、最大长度及冲突错误码。 |
| 编辑已撤销记录 | 编辑接口仅写“已撤销不可改（视业务）”，无法形成确定的前端约束。 | 保留编辑入口并以接口返回结果为准，后端拒绝时直接展示原因。 | 明确草稿、已发布、已撤销三种状态分别允许修改的字段。 |
| 发布重叠提示 `POST /api/v1/admin/controls/:id/publish` | `overlap` 只说明包含 `{kind,id,name}`，未定义 `kind` 枚举、几何判定规则及是否可能重复。 | 发布成功后按返回数组原样展示，明确提示不会自动停用设施。 | 定义对象类型枚举、坐标/边界接触判定、去重规则及数组为空的稳定返回形式。 |

## 内容管理

| 模块 / 接口 | 差异或问题 | 当前前端处理 | 建议后端确认 |
| --- | --- | --- | --- |
| 媒体上传 | `CmsController` 没有封面图、Banner 图片、公告附件或富文本图片上传接口，但原型包含这些上传能力，且活动封面与 Banner 图片为必填。 | 上传区域保留原型样式但禁用本地文件选择；已有远程 URL 只读展示并原样保留，不生成或提交 `blob:`/Base64。新建活动和 Banner 在无远程媒体时禁止提交，资讯及无附件公告仍可创建。 | 提供统一文件上传接口，明确鉴权、文件类型/大小、返回 URL、资源删除和孤儿文件清理规则。 |
| 内容优先级 | 原型明确“数值越小越靠前”，但 `ContentVO.priority` 文档写“越大越前”。 | 四个页签均按原型执行“置顶 → 优先级升序 → 时间倒序”。 | 统一后台列表、H5 展示和接口文档的排序方向，避免同一配置在两端顺序相反。 |
| 内容列表 `GET /api/v1/admin/contents` | 页面还需按置顶、启停和活动时间状态筛选；接口只支持关键字、内容类型、发布状态、数据来源，也未约定跨页稳定排序。 | 将接口支持的条件提交后端，每页 100 条自动拉取全部分页，再执行前端筛选、排序和 20 条分页。 | 增加置顶、启停、活动状态/时间范围参数并明确稳定排序；数据增长后避免全量拉取。 |
| Banner / 高优提示列表 | 页面需要按启停状态筛选并按原型顺序展示，两个列表接口没有状态筛选参数或稳定排序约定。 | 拉取当前接口条件下的全部分页，再在前端筛选和按优先级升序、更新时间倒序排列。 | 增加 `status` 筛选并明确默认稳定排序。 |
| 内容导出 `GET /api/v1/admin/contents/export` | 后端导出不接受页面筛选条件；Banner 和高优提示也没有导出接口，而原型只在活动、资讯通知页显示导出。 | 活动和资讯通知按当前筛选的完整结果生成 UTF-8 CSV；Banner、高优提示不显示导出。服务中保留后端全量内容 CSV 封装。 | 若数据量增大，给内容导出增加列表同款筛选参数；确认 Banner/高优提示是否长期不需要导出。 |
| 定时发布 `POST /api/v1/admin/contents/:id/publish` | 新增/更新接口不直接保存发布时间；按原型保存定时发布需要先保存草稿再调用发布接口，两次请求存在部分成功风险。 | 先新增/更新草稿字段，再携带本地时间格式的 `publish_at` 调用发布；任一步失败都刷新接口数据并展示后端原因。清空发布时间时调用撤回接口。 | 提供可原子保存内容和发布计划的接口，或定义两步操作的幂等、回滚及失败恢复规则。 |
| 快捷置顶/启停 | 更新接口虽为 `PATCH`，仍要求 `title` 和 `content_type` 等基础字段；Banner/高优提示同样要求基础字段，快捷操作存在并发覆盖风险。 | 每次快捷操作先读取最新详情，再只携带接口要求的基础字段和目标状态更新。 | 支持真正字段级 `PATCH`，或提供置顶/启停专用接口及乐观锁版本号。 |
| 导航、清空与附件协议 | 原型只有“导航地址 / 经纬度”单输入，接口拆为地址和经纬度；更新接口未完整说明可空字段传 `null`、省略和空字符串的清空语义。附件对象的稳定子字段、文件排序及日期时区规则也不完整。 | 识别“经度, 纬度”后拆分提交，否则按地址提交；显式清空导航字段。附件使用详情返回的远程字段原样保留；日期按本地时间提交。 | 明确互斥导航字段、所有可空字段的更新语义、附件对象完整 Schema、时间格式与时区。 |
| 删除内容 `DELETE /api/v1/admin/contents/:id` | 删除失败未定义引用数量、引用类型、编号等结构化数据，前端无法在确认前可靠展示影响范围。 | 不做本地引用预检，二次确认后直接删除；失败时原样展示后端可读原因。 | 返回稳定的引用冲突业务码及结构化引用摘要，同时保留可读提示。 |
| 引用选项 | 内容选项最多返回 500 条且不可分页、不可搜索；数据超过上限后 Banner/高优提示可能无法选择目标。 | 按引用类型动态读取选项，已保存记录优先使用 `jump_title/ref_title` 回显失效或未加载目标。 | 支持关键字搜索和分页，或提供按 ID 批量回显接口。 |
| 数量上限与编号 | 原型限制 Banner 最多 8 条、高优提示最多 3 条；接口仅提到 H5 最多展示 2 条高优提示，未声明配置上限。三类接口的自动编号格式、唯一性和长度也未约定。 | 前端保留 8/3 条即时限制，编号只使用后端返回值，不在新增/编辑时提交。最终合法性仍以后端为准。 | 后端同步校验配置上限，并明确 Banner、高优提示、内容编号的生成与唯一性规则。 |

## 数据看板

| 模块 / 接口 | 差异或问题 | 当前前端处理 | 建议后端确认 |
| --- | --- | --- | --- |
| 现状分布 `GET /api/v1/admin/stats/distribution` | 只返回收费类型、管制状态、活动状态和停车余位汇总，没有支持原页面分布下钻的对象明细接口。 | 分布图和停车柱状图仅展示汇总，移除点击样式、明细弹窗和模拟对象列表。 | 若运营后续需要追溯对象，增加按分布类型和切片查询明细的分页接口。 |
| 指标明细 `GET /api/v1/admin/stats/details` | 明细接口只按指标编码返回原始埋点，无法按 PV / UV 维度区分明细；UV 去重口径也无法由单页事件直接还原。 | PV / UV 切换只影响趋势图，原始事件表和导出始终使用同一埋点明细接口。 | 明确各指标的事件白名单、PV / UV 聚合口径；如需 UV 明细，提供专用去重结果接口。 |
| 时间窗协议 | `TimeWindow.end` 是开区间，但页面按自然日展示；接口时间均为 RFC3339，文档未明确所有统计、活动和埋点时间统一采用的业务时区。 | 以 `Asia/Shanghai` 解析，展示结束时间前 1 毫秒所在自然日；活动筛选只提交 `activity_id`，最终范围使用后端返回时间窗。 | 明确预设、自定义范围、活动时间窗和逐日分桶采用的时区，以及夏令时或跨时区数据处理规则。 |
| 分布项标识与快照时间 | `DistSlice`、`ParkingBar` 只有名称和数值，没有稳定 ID；分布接口也没有 `as_of`，重名或改名后无法稳定识别，页面无法显示独立更新时间。 | 前端按返回顺序生成仅用于渲染的 key，停车占用数使用 `capacity - remain`；页面不把生成 key 当业务 ID。 | 返回稳定业务 ID 和分布快照时间，并约定名称唯一性、空数组和停车余位大于容量时的处理规则。 |
| VR 同步 `POST /api/v1/admin/stats/vr-sync` | 只支持按 `yun720` 类型全量同步，无法按作品同步；且需要额外的 `integration:operate` 权限，前端菜单权限中没有可靠的动作级状态可用于预判。 | 仅在板块标题提供“同步全部 720 云”；权限不足、同步失败及自动停用均直接展示后端原因和摘要，完成后重新读取全部作品。 | 若需要细粒度操作，补充按作品同步接口与动作权限下发；明确同步互斥、超时、部分成功和重试语义。 |
| 明细导出 `GET /api/v1/admin/stats/details/export` | 文档说明最多 5000 条，但未说明超过上限时会截断、拒绝还是返回部分文件，也没有返回是否截断的标记。 | 直接下载后端 UTF-8 CSV 与响应文件名，不再前端全量拉取或设置 5 万条限制；失败时展示后端错误。 | 超过上限时返回明确业务错误，或在文件名/响应头中标识截断及实际导出条数，避免误认为完整数据。 |
| 埋点敏感与扩展数据 | `AnalyticsEvent` 返回设备 ID、IP 和 `extra_json`，但未约定管理端展示范围、脱敏策略、扩展 JSON Schema 与数据保留期限。 | 表格仅展示设备 ID、页面和关联对象，不展示 IP 与完整扩展 JSON；类型中保留原字段供协议兼容，CSV 按接口原样下载。 | 明确设备/IP 的脱敏和权限、导出审计、数据保留期限，并为 `extra_json` 提供按事件区分的结构定义。 |
| 指标与 VR 展示口径 | KPI 未返回独立数据来源字段；`change=null` 同时可能表达“新增”或无可比数据。VR 接口不返回排名，且“启用中的作品”仍包含状态/失效字段，列表稳定顺序未约定。 | 指标来源统一显示“统计埋点”，环比优先使用 `change_text`；VR 按 PV 倒序生成排名，并展示接口状态。 | 为 KPI 返回来源或口径版本；保证 `change_text` 稳定可展示；明确 VR 返回范围、失效数据语义和稳定排序。 |

## 外部数据对接

| 模块 / 接口 | 差异或问题 | 当前前端处理 | 建议后端确认 |
| --- | --- | --- | --- |
| 连接测试 | 原型要求保存前可测试 API 地址和密钥，但 `IntegrationController` 没有连接测试接口；浏览器直连上游还会暴露密钥并受 CORS 限制。 | 保留“测试连接”入口但禁用，明确提示等待后端接口；不模拟连接成功，也不阻止用户直接保存配置。 | 提供服务端连接测试接口，明确超时、鉴权失败、响应协议错误和测试操作审计。 |
| 全部同步 | 原型要求一次触发所有启用源并返回成功/失败汇总，接口只有按类型同步和单源同步，没有原子的全部同步端点。 | 保留“全部同步”按钮但禁用，避免前端串行请求产生中途中断、刷新页面后丢失汇总等问题。 | 提供全部同步任务接口及任务状态/结果查询，明确执行顺序、并发限制、部分失败和重复触发语义。 |
| 对接源列表筛选 `GET /api/v1/admin/integrations` | 原型包含同步状态筛选，但列表接口只支持关键字和对接类型，也未声明默认稳定排序。 | 关键字和类型提交服务端；同步状态筛选保留但禁用，列表使用服务端分页和返回顺序。 | 增加 `last_sync_status/status` 筛选并明确稳定排序，例如失败优先、更新时间倒序或编号升序。 |
| 同步日志 `GET /api/v1/admin/integrations/logs` | 原型要求按类型、结果、日期范围筛选并导出；接口只支持 `source_id/result`，也没有日志导出端点。 | 结果筛选和 20 条服务端分页正常使用；类型、日期范围和导出控件保留但禁用，不全量拉取日志做前端补偿。 | 增加 `source_type/start/end` 参数及原始 UTF-8 CSV 导出，并保证筛选参数与列表一致。 |
| 日志对接源信息 | `SyncLog` 仅返回 `source_id`，没有对接源编号和名称；删除或不在当前页的源无法直接回显。 | 打开日志时后台补充读取对接源缓存；无法匹配时显示 `对接源 #<source_id>`。 | 日志返回 `source_code/source_name` 快照，保证源改名或删除后仍可追溯当时信息。 |
| 快捷启停 `PATCH /api/v1/admin/integrations/:id` | 更新接口要求名称和类型等基础字段，没有状态专用接口或版本字段；快捷启停存在并发覆盖风险。 | 启停前读取最新详情，再携带必要基础字段和目标状态更新；密钥留空且不提交。 | 增加启停专用接口或支持真正字段级 PATCH，并提供更新时间/版本号进行乐观锁校验。 |
| API 地址与密钥必填 | 原型要求 API 地址和新建密钥必填，但接口允许二者为空，并说明空地址或 `mock` 会走 Mock；生产环境是否允许 Mock 未约定。 | 按原型校验新建时 URL、密钥必填，URL 仅接受 HTTP/HTTPS；编辑密钥留空表示保留原值。 | 区分生产与测试模式，明确空值/Mock 的开放环境、权限和审计要求，并统一接口必填约束。 |
| 密钥更新与清空 | 详情只返回 `api_key_masked`，更新说明空 `api_key` 表示不修改，但未说明是否支持主动清空密钥及掩码为空的语义。 | 编辑时不把掩码写回输入框；用户留空则完全省略 `api_key`，本轮不提供清空密钥功能。 | 明确密钥清空、轮换、过期和失效提示协议，避免空字符串产生歧义。 |
| 类型唯一与自动停用 | 原型要求同类型仅一个启用源、连续 3 次失败自动停用；接口只返回连续失败次数和 `disabled`，未完整说明唯一约束、失败阈值及恢复规则。 | 前端展示规则说明和后端结果，不做可绕过的本地最终校验；失败或自动停用直接展示后端摘要。 | 明确类型唯一的业务错误码、失败计数范围、成功后是否清零、自动停用阈值及重新启用后的恢复行为。 |
| 存量类型 | 接口允许列表展示 `host_activity/police_control/shuttle`，文档称其只读，但更新接口又写“存量可保留”，操作边界不完全一致。 | 存量类型不允许编辑或启停，仅在启用状态下保留单源手动同步；新增只允许 `parking/yun720`。 | 明确存量类型是否允许修改基础配置、启停、删除和手动同步，并统一接口文档描述。 |
